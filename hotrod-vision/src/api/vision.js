// src/api/vision.js
// HotRod Vision is being rebuilt. This placeholder prevents calls to localhost:3000.
export async function identifyCar(_file) {
  throw new Error("HotRod Vision is temporarily disabled while we rebuild it.");
}